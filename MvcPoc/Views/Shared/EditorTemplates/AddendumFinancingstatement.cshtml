@model MvcPoc.Web.Models.Addendum.Ucc1AddendumModel.AddendumFinancingStatement

<table>
    <tr>
        @foreach (var value in Enum.GetValues(typeof(MvcPoc.Web.Models.Addendum.Ucc1AddendumModel.AddendumFinancingStatement)))
        {
            <td style="width:20px">
                @Html.RadioButtonFor(modelitem => modelitem, value)@Html.Label(value.ToString())
            </td>
        }
    </tr>
</table>