@model MvcPoc.Web.Models.Addendum.Ucc1AddendumModel.AddendumAlternateFilingType

<table>
    <tr>
        @foreach (var value in Enum.GetValues(typeof(MvcPoc.Web.Models.Addendum.Ucc1AddendumModel.AddendumAlternateFilingType)))
        {
            <td style="width:20px">
                @Html.RadioButtonFor(modelitem => modelitem, value)@Html.Label(value.ToString())
            </td>
        }
    </tr>
</table>